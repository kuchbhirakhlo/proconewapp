import { softwareDevelopmentServicesMetadata } from "@/lib/seo-config"

export const metadata = softwareDevelopmentServicesMetadata

export default function BusinessLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}