import { adcaCourseMetadata } from "@/lib/seo-config"

export const metadata = adcaCourseMetadata

export default function AdcaCourseLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}