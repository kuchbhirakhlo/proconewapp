import { webDevelopmentCourseMetadata } from "@/lib/seo-config"

export const metadata = webDevelopmentCourseMetadata

export default function WebDevelopmentLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}